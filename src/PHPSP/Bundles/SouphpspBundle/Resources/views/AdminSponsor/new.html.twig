<h1>Sponsor creation</h1>

<form action="{{ path('admin_sponsor_create') }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <p>
        <button type="submit">Create</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('admin_sponsor') }}">
            Back to the list
        </a>
    </li>
</ul>
